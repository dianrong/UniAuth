define(['../../utils/constant'], function (constant) {
    /**
     * A module representing a User controller.
     * @exports controllers/User
     */
    var Controller = function ($scope, TagService) {
        //
    };

    return {
        name: "TagController",
        fn: ["$scope", "TagService", Controller]
    };

});
